[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = [
  "rm -rf .next/cache",
  "npm run build"
]

[variables]
SKIP_ENV_VALIDATION = "true"

[start]
cmd = "npm start"
